# SQL-01
Q:find the employees who earn more than their managers.
select em.Name
from Employee em
    join Employee ma
    on em.Id=ma.ManagerId -->  on em.ManagerId=ma.Id 
where em.Salary>ma.Salary

#new skill (ahow Number)
select count(Email) as num

196
DELETE p2 FROM Person p1 JOIN Person p2 
ON p2.Email = p1.Email WHERE p2.Id > p1.Id;
